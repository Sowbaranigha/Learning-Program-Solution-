# Set custom server port (optional; default is 8080)
server.port=8087

# Enable console logs for debugging
logging.level.org.springframework=INFO

# Optional: show full request logs
logging.level.web=DEBUG
