# Change the server port from default 8080 to another port (e.g. 9090)
server.port=9091

# Optional: Enable more detailed logging (helpful during development)
logging.level.org.springframework=INFO

# Optional: Custom application name (for logs)
spring.application.name=SecureRestApi
